id: agent-spaces.agent-harness-runtime-boundary
kind: invariant
status: active
owner_project: agent-spaces
scope: first-party agent-harness composition compilation release binding and broker ownership
predicate: >
  Amendment T-08680 (2026-09-21, ruled by Lance) supersedes the former
  agent-harness broker-child topology and preserves the direct resource and
  runtime laws below. The agent-harness and agent-harness-tmux driver identities
  are served by one identity-bound agent-harness executable in an immutable ASP
  release. ASPC interprets the mutable agent declaration and emits a complete
  hash-covered broker profile. aspd selects only an inspected binding and
  freezes the exact release worker in executionRelease.worker.executable; both
  bindings name that executable and hostedDrivers is positive evidence derived
  from the release binding table. HRC launches that worker generically and
  retains placement, terminal-lease, lifecycle, routing, and durable-messaging
  authority, but it does not build an agent-harness plan, register either
  driver, import the first-party runtime, infer a worker, or speak an
  agent-harness-private protocol. Both profiles use the native-worker process
  alternative: no process command or arguments exist and harnessTransport is
  native-worker, meaning the already-external release worker directly owns the
  selected runtime and speaks the standard harness-broker protocol.
  Native-worker is distinct from HRC-local in-process execution and from a
  driver-launched pty or JSON-RPC child. Headless selects agent-harness with no
  terminal and owns the Pi runtime in the worker. Interactive selects
  agent-harness-tmux and HRC starts that same worker itself in the leased tmux
  surface; the worker owns the Pi runtime, embedded TUI, broker server,
  broker-delivered input, operator-entered input, invocation identity, and one
  authoritative event sequence in one process. No agent-harness child,
  agent-harness-control/v1 socket, session-config or turn-begin handshake,
  event relay, or second sequencer exists. Both drivers require the semantic
  agent block and a Pi SDK model binding, while the outer runtime discriminator
  remains the distinct first-party agent-harness value. The shared Pi driver
  owns auth, permission, continuation, mapping, interruption, and disposal; an
  embedded presentation may observe operator turns but cannot allocate a
  second broker sequence. Earendil InteractiveMode may be used in the worker
  only through a non-exiting embedded lifecycle that restores the terminal,
  unregisters its handlers, returns on clean leave, leaves runtime disposal to
  the driver, and never exits the broker process. The direct runtime continues
  to use AgentSpacesResourceLoader as its sole resource-delivery boundary. It
  resolves ordered ASP sources and contextual prompt, reminder, skills, model,
  auth, permission, and environment without a harness adapter, compiler-built
  merged Pi bundle, ambient Pi discovery, or generated external-harness home;
  every replacement session reloads that boundary. Foreground asp run remains
  a separate local TUI/print surface using the same runtime factory. Existing
  external-harness, generic Pi SDK, Arris, Muse, and Desktop routes retain their
  own contracts, and HRC retains all non-execution semantic authority named by
  their active records.
authority: T-08680 native-worker amendment ruled by Lance 2026-09-21; prior T-08563 revision 5 approved by daedalus EN-13318 and hrcchat#21059; T-08594 Desktop amendment ruled by Lance
source:
  - wrkq:T-08680
  - docs/proposals/agent-harness-native-release-worker.md
  - docs/proposals/agent-harness.md
  - docs/proposals/agent-harness-hrc-interactive.md
  - wrkq:T-08594
  - wrkq:T-08563
  - wrkq:EN-13318
  - hrcchat#21059
  - hrcchat#20779
  - docs/proposals/agent-resource-parity-verifier.md
  - harness/agent-harness-runtime/src/index.ts
  - harness/agent-harness/src/cli.ts
  - harness/agent-harness/src/broker/driver.ts
  - harness/harness-broker-pi-sdk/src/driver.ts
  - scripts/asp-release.ts
required_tests:
  - both agent-harness profiles use the native-worker shape and reject a process command arguments in-process pty jsonrpc-stdio and private-control declarations
  - noninteractive intent selects agent-harness without a terminal while interactive intent selects agent-harness-tmux with an HRC-owned tmux terminal surface
  - inspection preserves the distinct agent-harness Pi-family runtime rather than falling through to Codex or generic Pi SDK
  - compiled profiles preserve semantic agent identity model provider auth reasoning permission continuation and correlation inputs under the invocation hash
  - one identity-bound release executable advertises both drivers reports its embedded release identity and is selected only through explicit inspected bindings
  - release closure inspection rejects checkout paths identity mismatch digest drift missing inventory and a binding to another executable
  - headless broker input runs in the worker-owned Pi runtime and emits directly through the ordinary DriverContext without an agent-harness child
  - interactive broker and operator inputs use the same live session real DriverContext mapper invocation identity and monotonic broker sequence
  - interactive clean leave restores the terminal without process exit leaves the broker responsive and defers runtime disposal to broker stop
  - no active implementation source or serialized runtime contract declares agent-harness-control/v1 control sockets turn-begin or session-config frames or an agent-harness event relay
  - isolated aspd preparation returns the exact immutable worker and positive hostedDrivers then a real headless turn completes from that executable
  - a real tmux proof launches the exact worker in the leased surface completes broker and operator turns survives detach and remains controllable until stop
  - direct source resolution and every replacement session preserve the custom ASP resource loader with no adapter lowering generated merged Pi bundle or ambient discovery
  - foreground TUI and print plus existing external generic Pi Muse Arris and Desktop routes regress cleanly
last_verified:
  date: 2026-09-21
  evidence: owner-approved T-08680 native-worker design recorded; implementation evidence pending
reopen_when:
  - HRC gains agent-harness-specific planning registration runtime imports executable selection or a private child protocol
  - either driver uses in-process pty jsonrpc-stdio or a serialized process command instead of native-worker
  - the interactive route launches a TUI child or gains a second identity mapper sequencer or event relay
  - the embedded TUI can exit the broker process dispose the runtime independently or leave terminal handlers installed
  - either driver is inferred rather than positively bound to the inspected immutable executable
  - the direct runtime requires a harness adapter compiler-built Pi bundle or ambient Pi resource discovery
  - a replacement session can lose the custom ASP resource loader
  - foreground execution attaches to an HRC runtime or stops sharing the first-party runtime factory
supersedes:
  - agent-spaces.raspc-migration-contract
superseded_by: null
