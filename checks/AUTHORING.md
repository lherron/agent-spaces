# Check Authoring Ledger

| check | rule | why | bad | good | exception | rung | sunset-condition |
|-------|------|-----|-----|------|-----------|------|------------------|
| check-boundaries.ts | ARCH-L2 layer-direction guard | Preserves the repo split by keeping protected layers from importing forbidden outer layers. | A lower architecture layer imports an HRC, Codex, or otherwise forbidden package edge. | Move the shared API lower, invert the dependency, or remove the forbidden edge. | Reviewed rule-set change with architecture rationale; no inline silence. | GUARD | Remove when repo layering is enforced by package boundaries and forbidden cross-layer imports are impossible by construction. |
| check-runtime-contract-harness-boundaries.ts | RUNTIME-CONTRACT-HARNESS boundary guard | Keeps the pre-HRC broker harness proving compiler output without depending on HRC or Codex internals. | Harness code imports HRC, Codex driver/session internals, direct builders, or split start calls. | Use compiler, broker-client, protocol, and startInvocationFromRequest surfaces only. | Reviewed harness boundary rule change with rationale; compatibility tests stay outside the scanned surface. | GUARD | Remove when the runtime-contract harness is replaced by an external conformance package that cannot import repo internals. |
| check-manifest-edges.ts | MANIFEST declared-dependency guard | Makes package manifests reflect every source import so isolated installs and tooling see real edges. | Source imports a workspace package that is absent from the package manifest dependencies. | Declare the imported workspace package in the owning package manifest before using it. | Reviewed rule change only if a package manager starts enforcing this edge automatically. | GUARD | Remove when the workspace package manager rejects undeclared workspace imports during install or typecheck. |
| check-suppressions.ts | SUPPRESSIONS ticketed-exception guard | Keeps new or changed lint and type suppressions in the reviewed trust pool. | Add biome, eslint, ts-ignore, or ts-expect-error suppression without adjacent EXCEPTION(T-####). | Remove the suppression or add a reviewed EXCEPTION(T-####): reason. | Baseline update only for reviewed grandfather or reset changes. | GUARD | Remove when suppression policy is enforced by the primary linter with ticketed exception metadata. |
| check-public-surface.ts | PUBLIC-SURFACE contract coverage guard | Ensures new public barrel exports carry tests or explicit tracked waivers before becoming compatibility promises. | Export a new ratified package symbol or command surface without contract coverage or ticketed waiver. | Add symbol-level coverage, regenerate the reviewed baseline, or add CONTRACT-EXEMPT/TACIT(T-####). | Adjacent CONTRACT-EXEMPT(T-####) or TACIT(T-####) with a non-junk reason. | GUARD | Remove when public API extraction and coverage mapping are enforced by generated contract tests at package publish time. |
| check-doc-reachability.ts | DOC-REACHABILITY routed-link guard | Keeps routed AGENTS.md markdown links reachable for operators following local instructions. | Link to a missing file, target outside the doc root, or missing heading anchor. | Correct the link, create the target file, or add the referenced heading. | Reviewed checker change only if the router doc contract changes. | GUARD | Remove when routed agent docs are generated from validated route metadata rather than hand-authored markdown links. |
| check-rule-authoring.ts | RULE-AUTHORING ledger drift guard | Makes every verify-gating check document its rule, rationale, examples, exception path, rung, and sunset trigger. | Add a bun scripts/check-*.ts line to just check without a matching AUTHORING.md row. | Add or update the ledger row with all required cells before wiring the check into verify. | Reviewed checker change only for parser support or schema changes; no self-exemption. | GUARD | Remove when verify-gating check registration requires structured authoring metadata before a check can run. |
