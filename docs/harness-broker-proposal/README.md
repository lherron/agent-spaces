# Harness Broker Architecture Proposal

Open `index.html` to browse the proposal. The package includes detailed HTML pages for package placement, boundaries, protocol, events, process model, compile schema, input/control semantics, migration from `exec.ts`, and risks/implementation plan.
