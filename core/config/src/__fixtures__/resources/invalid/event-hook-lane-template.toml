schema = 1
name = "lane-template"
enabled = true

[event]
source = "wrkq"

[match]
event = ["updated"]
project_scope_id = "agent-spaces"
kind = "task"

[target]
project = "{{ project_scope_id }}"
agent = "smokey"
lane = "{{ lane }}"
task = "{{ticket_id}}"

[input]
content = "Run smoke tests."

[originPolicy]
agent = "deny"

[cooldown]
seconds = 300
