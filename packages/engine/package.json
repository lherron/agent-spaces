{
  "name": "@agent-spaces/engine",
  "version": "0.0.1",
  "description": "High-level orchestration engine for Agent Spaces v2",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "lint": "biome check .",
    "lint:fix": "biome check --write ."
  },
  "dependencies": {
    "@agent-spaces/core": "workspace:*",
    "@agent-spaces/git": "workspace:*",
    "@agent-spaces/claude": "workspace:*",
    "@agent-spaces/resolver": "workspace:*",
    "@agent-spaces/store": "workspace:*",
    "@agent-spaces/materializer": "workspace:*",
    "@agent-spaces/lint": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.3"
  }
}
