schema = 1
name = "fresh-session-invalid-type"
enabled = true
freshSession = "true"

[trigger]
cron = "0 * * * *"

[target]
project = "agent-spaces"
agent = "smokey"

[input]
content = "Run with a fresh context."
